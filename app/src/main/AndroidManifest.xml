<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ondo100"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".feature.kakao.KakaoActivity"
            android:exported="false" />
        <activity
            android:name=".feature.think.ThinkActivity"
            android:exported="false" />
        <activity
            android:name=".feature.message.MessageActivity"
            android:exported="false" />
        <activity
            android:name=".feature.call.CallActivity"
            android:exported="false" />
        <activity
            android:name=".feature.bank.BankActivity"
            android:exported="false" />
        <activity
            android:name=".feature.game.GameActivity"
            android:exported="false" />
    </application>

</manifest>